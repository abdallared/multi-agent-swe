# ============================================================
# AI Software Company - Dependencies
# Pure Ollama local stack — no cloud LLM dependencies needed
# ============================================================

# Core
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP (for Ollama API calls)
requests==2.31.0
httpx==0.26.0

# Web UI Backend
fastapi==0.108.0
uvicorn[standard]==0.25.0
python-multipart==0.0.6
websockets==12.0

# Security (used in generated projects & system)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# Database (used in generated projects)
sqlalchemy==2.0.25

# Data utilities
pyyaml==6.0.1
tenacity==8.2.3
rich==13.7.0

# Testing (for this system's own tests)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
coverage==7.4.0
